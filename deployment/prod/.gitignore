# Environment files with secrets
.env                    # Generated by Terraform, contains all secrets

# Terraform configuration with user-specific values
terraform.tfvars        # User's domain and config (check in terraform.tfvars.example instead)

# Terraform state (contains secrets in plaintext!)
*.tfstate
*.tfstate.*
*.tfstate.backup
.terraform/
.terraform.lock.hcl
terraform-outputs.json
